export type Employer = {};

export type User = {
  username: string;
  firstName: string;
  lastName: string;
  email: string;
  password: string;
};

export type Job = {};

export type Credentials = {
  username: string;
  password: string;
};
